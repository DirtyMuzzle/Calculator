#include <cmath>
using namespace std;
int AbsoluteValue(int a) {
	if (a >= 0) {
		return a;
	}
	else {
		return -a;
	}
}
double SquareRoot(double a) {
	return sqrt(a);
}
double ReciprocalFunction(double a) {
	return 1 / a;
}